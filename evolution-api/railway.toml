[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile"

[deploy]
# healthcheck temporarily disabled for initial migration deploy
# healthcheckPath = "/health"
# healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
